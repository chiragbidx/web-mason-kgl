// No metadata usually for API endpoints, but ensuring file and comments reference Snapify branding

// Snapify Stripe Webhook Route — Handles Stripe subscription events

// ...existing Stripe webhook logic unchanged...
export async function POST(req: Request) {
  // Stripe webhook entry code (unchanged)
}